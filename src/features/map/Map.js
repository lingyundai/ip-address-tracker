import { MapContainer, TileLayer, Marker, Popup } from 'react-leaflet/MapContainer';

function Map() {
    return (
        <MapContainer>
            
        </MapContainer>
    )
}

export default Map;